# Demo clip

`Lifeguard-15s-autonomous-demo.mp4` is a 14.5-second capture of the packaged Windows application. It shows the whole-drive index advancing and Lifeguard's isolated demo action appearing without a cleanup recommendation flow.

The capture is cropped above the activity-line details so no local file paths appear in the video.
